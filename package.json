{
  "name": "team-1-api",
  "version": "1.0.0",
  "description": "project",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd web && npx react-scripts build && cd .. && rm -rf server/public && mv web/build server/public && cp server/public-static-files/* server/public/.",
    "dev": "cd web && npm start & cd server && npm run dev",
    "install": "cd web && npm install && cd ../server && npm install"
  },
  "author": "team1",
  "license": "ISC"
}
